#!/usr/bin/env bash

if [ -z "$1" ]
then
    echo "usage: cleanup-data-files <nuber of days old>"
    exit 1
fi

# delete files older than $1 days
find /opt/lerc/lerc_server/data -type f -mtime +$1 -delete
